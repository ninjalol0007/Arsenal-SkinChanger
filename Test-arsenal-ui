local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "BSGL " .. Fluent.Version,
    SubTitle = "by Test",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true, -- The blur may be detectable, setting this to false disables blur entirely
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl -- Used when theres no MinimizeKeybind
})

local Tabs = {
    Main = Window:AddTab({ Title = "Gum", Icon = "" }),
    Settings = Window:AddTab({ Title = "Rw", Icon = "settings" })
}


    Tabs.Gum:AddButton({
        Title = "Gum",
        Description = "Auto Gum",
        Callback = function()
      getgenv().farm = true 
    while getgenv().farm do
   task.wait()
 local args = {
	"BlowBubble"


game:GetService("ReplicatedStorage"):WaitForChild("NetworkRemoteEvent"):FireServer(unpack(args))
end })

    Tabs.Rw:AddButton({
        Title = "1",
        Description = "Very important button",
        Callback = function()
       getgenv().collect = true 
while getgenv().collect do
task.wait()
local args = {
	"ClaimAchievement",
	1
}
game:GetService("ReplicatedStorage"):WaitForChild("NetworkRemoteEvent"):FireServer(unpack(args))

end})

Tabs.Rw:AddButton({
        Title = "Button2",
        Description = "Very important button",
        Callback = function()
       getgenv().collect = true 
while getgenv().collect do
task.wait()
local args = {
	"ClaimAchievement",
	2
}
game:GetService("ReplicatedStorage"):WaitForChild("NetworkRemoteEvent"):FireServer(unpack(args))

end})

Tabs.Rw:AddButton({
        Title = "Button3",
        Description = "Very important button",
        Callback = function()
       getgenv().collect = true 
while getgenv().collect do
task.wait()
local args = {
	"ClaimAchievement",
	3
}
game:GetService("ReplicatedStorage"):WaitForChild("NetworkRemoteEvent"):FireServer(unpack(args))

end})

Tabs.Rw:AddButton({
        Title = "Button4",
        Description = "Very important button",
        Callback = function()
       getgenv().collect = true 
while getgenv().collect do
task.wait()
local args = {
	"ClaimAchievement",
	4
}
game:GetService("ReplicatedStorage"):WaitForChild("NetworkRemoteEvent"):FireServer(unpack(args))

end})
