local Tab = Window:CreateTab("Tab Example", 4483362458) -- Title, Image

local Tab = Window:CreateTab("Skin", "rewind")

local Section = Tab:CreateSection("Test")

Section:Set("Section Example")


local Divider = Tab:CreateDivider()

Divider:Set(false) -- Whether the divider's visibility is to be set to true or false.

Rayfield:SetVisibility(false)

Rayfield:IsVisible()

Rayfield:Destroy()

Window.ModifyTheme('Default')


Rayfield:Notify({
   Title = "test",
   Content = "Notification Content",
   Duration = 3.5,
   Image = 4483362458,
})

local Button = TabSkin:CreateButton({
   Name = "TEst",
   Callback = function()
   game.Players.LocalPlayer.Equipped.Value = "Creator"
   end,
})

